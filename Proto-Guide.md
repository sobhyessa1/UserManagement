# 🎓 دليل المُبتدئين لفهم ملفات الـ Proto (`.proto`) 🚀

أهلاً بيك يا بطل! 👋
في الدليل ده، هنفصص سوا يعني إيه ملف `.proto`، وليه بنستخدمه، وإزاي نكتبه صح، كأننا بنحكي حدوتة.

---

## 🤔 يعني إيه أصلاً `.proto`؟

تخيل إنك بتتكلم عربي، وصاحبك بيتكلم صيني. عشان تفهموا بعض، محتاجين **لغة وسيطة** أو **مترجم**.
في عالم البرمجة:
- الـ **User Service** (ممكن يكون مكتوب بـ C#)
- الـ **Order Service** (ممكن يكون مكتوب بـ Python أو Java)

عشان يكلموا بعض، بنستخدم لغة مشتركة اسمها **Protocol Buffers** (أو Proto اختصاراً).
ملف الـ `.proto` هو **"القاموس"** أو **"العقد"** اللي بيقول للاتنين دول هيتكلموا إزاي.

---

## 🏗️ مكونات الملف (التشريح)

أي ملف proto بيتكون من 3 أجزاء رئيسية، زي جسم الإنسان (رأس، وجذع، وأطراف):

### 1️⃣ الرأس (الإعدادات)
```protobuf
syntax = "proto3"; 
option csharp_namespace = "UserManagement.API.Protos";
```
- **`syntax = "proto3"`**: زي ما بتقول "أنا بكتب بإصدار سنة 2023"، عشان الكمبيوتر يعرف القواعد الحديثة.
- **`option csharp_namespace`**: بتقول للكمبيوتر: "لما تحول الملف ده لـ C#، حطه في الـ Namespace الفلاني".

### 2️⃣ الجذع (الخدمات - Services)
ده الجزء اللي بيوصف **"الأفعال"**. زي الـ `Interface` في C#.
```protobuf
service UserGrpc {
  rpc GetUserById (GetUserRequest) returns (UserResponse);
}
```
- **`service`**: يعني "خدمة".
- **`rpc`**: اختصار لـ **Remote Procedure Call** (يعني "يا عم السيرفر، شغل الدالة دي عندك").
- **القاعدة الذهبية**: أي دالة في gRPC لازم تاخد **رسالة واحدة** وترجع **رسالة واحدة**. حتى لو مش محتاج تبعت بيانات، لازم تبعت رسالة فاضية!

### 3️⃣ الأطراف (الرسائل - Messages)
ده الجزء اللي بيوصف **"البيانات"**. زي الـ `Class` أو `DTO` في C#.
```protobuf
message UserResponse {
  int32 id = 1;
  string name = 2;
}
```

---

## 🔢 سر الأرقام (`= 1`, `= 2`) 🧩

ليه بنكتب `= 1` جنب الداتا؟ دي مش قيمة ابتدائية!
دي **Tags** (أرقام تعريفية).

**تخيلها كده:**
الكمبيوتر لما بيجي يبعت الداتا في السلك، مش بيبعت كلمة "name" ولا كلمة "email" عشان يوفر مساحة. هو بيبعت:
> "الحقل رقم **1** قيمته 50، والحقل رقم **2** قيمته 'Ahmed'".

عشان كده:
1.  **ممنوع التكرار**: مينفعش حقلين ياخدوا نفس الرقم.
2.  **ممنوع التغيير**: لو اديت الـ `name` رقم 2، يفضل طول عمره 2. لو غيرته، البرامج القديمة مش هتفهمك.

---

## 🔄 جدول التحويل (القاموس)

إزاي تحول أنواع البيانات من C# لـ Proto؟

| لو في C# عندك | اكتب في Proto | ملاحظات |
| :--- | :--- | :--- |
| `int` | `int32` | للأرقام الصحيحة العادية |
| `long` | `int64` | للأرقام الكبيرة جداً |
| `string` | `string` | للنصوص |
| `bool` | `bool` | صح أو غلط |
| `float` | `float` | أرقام عشرية صغيرة |
| `double` | `double` | أرقام عشرية دقيقة |
| `List<int>` | `repeated int32` | كلمة **repeated** يعني ليستة (Array) |
| `DateTime` | `google.protobuf.Timestamp` | محتاجة استيراد مكتبة خاصة |

---

## 💡 مثال عملي على مشروعنا

إحنا عايزين الـ Order Service يسأل: "هل اليوزر رقم 5 موجود؟".

### الخطوة 1: نفكر في الـ Request (السؤال)
إحنا محتاجين نبعت `ID` (رقم صحيح).
```protobuf
message CheckUserExistsRequest {
  int32 user_id = 1; // الـ ID اللي بنسأل عليه
}
```

### الخطوة 2: نفكر في الـ Response (الإجابة)
إحنا محتاجين رد بـ "اه" أو "لا" (True/False).
```protobuf
message CheckUserExistsResponse {
  bool exists = 1; // النتيجة
}
```

### الخطوة 3: نكتب الـ Service (الفعل)
```protobuf
service UserGrpc {
  rpc CheckUserExists (CheckUserExistsRequest) returns (CheckUserExistsResponse);
}
```

---

## � ملخص سريع
1.  الـ **Proto** هو "اللغة المشتركة".
2.  الـ **Service** هي "الأفعال" (Methods).
3.  الـ **Message** هي "البيانات" (Classes).
4.  الأرقام (`= 1`) مهمة جداً عشان السرعة، وممنوع تتغير.

جاهز نطبق الكلام ده في الكود؟ 😉
